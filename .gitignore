# 1. Python 垃圾文件和虚拟环境
__pycache__/
*.py[cod]
venv/
env/
.env
.DS_Store

# 2. IDE 配置文件（建议忽略，因为别人的 VSCode 配置可能和你不一样）
.vscode/
.idea/

# 3. 数据文件 (关键决策点)
# 作为一个 MLE 项目，通常不建议把原始数据(raw data)传到代码库
# 但是！因为 UCI 这个数据集很小，为了方便运行，可以 *不忽略* 它。
# 如果想更专业，可以忽略它，然后写个脚本自动下载。
# 建议：如果是展示，先注释掉下面这行（即允许上传），方便别人跑。
# data/raw/*.csv

# 4. 模型文件 (关键决策点)
# 同理，如果模型文件 < 50MB，建议传上去，这样 clone 下来直接就能跑 app.py。
# 如果模型很大 (>100MB)，必须忽略，否则 git push 会失败。
#  XGBoost 模型应该很小，所以建议 *注释掉* 下面这行（即允许上传）。
# models/*.joblib

# 5. Streamlit 缓存
.streamlit/